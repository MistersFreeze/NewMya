/**
 * Package entry. Luau consumers can require the compiled `out` tree from Rojo
 * or copy a bundled module once a bundler is adopted.
 */
export { mountKrokmouShell } from "./components/App";
export type { HubTheme } from "./theme";
export type { MountOptions } from "./components/App";
